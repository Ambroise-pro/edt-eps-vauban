rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lecture publique autorisée, écriture réservée aux utilisateurs authentifiés
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
